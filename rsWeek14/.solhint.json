{
  "extends": ["solhint:recommended"],
  "plugins": [],
  "rules": {
    "quotes": ["error", "double"],
    "avoid-throw": "off",
    "avoid-suicide": "error",
    "compiler-version": ["off"],
    "func-visibility": ["warn", { "ignoreConstructors": true }]
  }
}
